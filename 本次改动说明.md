# 本次改动说明（精简版）

本文档仅记录本轮已实现的功能与界面调整。

## 1. Admin 端：系统设置

### 1.1 键值设置
- 删除了 `tips` 的可见与可编辑入口（不再在“键值设置”中显示）。
- `team_max_student_count` 显示名改为：`宿舍最大人数`（底层 key 未改）。
- 新增键值项：`学生端导航栏系统名称`（key: `student_nav_system_name`），可直接在“键值设置”中编辑。

### 1.2 新增“系统样式”栏
- 新增“系统样式”分组，包含：
  - `登陆界面背景`：支持上传 `jpg/png`，最大 `10MB`。
  - `学生端背景颜色`：支持输入颜色代码。
  - `学生端 logo`：支持上传 `jpg/png`，最大 `10MB`。
- 新增后台上传接口：`POST /api/admin/system_style/upload?kind=login_bg|student_logo`。

### 1.3 问卷设计页（`/system/questionnaire`）
- 每道题右侧新增“调整分页”上下箭头（位于拖拽控件右侧）：
  - 上箭头：移动到上一分页末尾。
  - 下箭头：移动到下一分页开头。
  - 第一分页题目仅下箭头，最后分页题目仅上箭头。
- 分页列表每项右侧新增“调整分页位置”上下箭头：
  - 上箭头：与上一分页交换。
  - 下箭头：与下一分页交换。
  - 第一分页仅下箭头，最后分页仅上箭头。
- 修复交互冲突：点击“调整分页”箭头不再触发“题目属性”抽屉。

---

## 2. Student 端：导航与主页

### 2.1 导航栏与首页命名
- `公告与个人资料` 改名为 `主页`（页面标题与导航文案同步）。
- 新增“学生端导航栏系统名称”动态读取，左上角系统名可由 Admin 配置修改。

### 2.2 主页样式与头像
- 主页标题右侧显示“我的头像”（圆形）。
- 主页头像尺寸放大为原来的 3 倍。
- “编辑个人信息”新增头像上传入口（`jpg/png`，最大 `10MB`）。
- 修复“选择文件后仍显示未选择任何文件”的显示问题：现在会显示已选文件名。

### 2.3 学生头像统一策略
- 舍友大厅卡片与个人资料页头像统一为以下优先级：
  1) 自定义头像（上传）
  2) QQ 头像
  3) 默认头像
- 新增学生头像上传接口：`POST /api/student/avatar/upload`。
- `/api/student/userinfo`、`/api/student/team/recommend_teammates`、`/api/student/student/<id>` 已补充 `avatar_url` 返回。

---

## 3. Student 端：系统样式生效

- 登录页背景支持读取 Admin 设置（`login_bg_url`）。
- 学生端导航右侧新增圆形 logo 区域，并支持显示 Admin 上传的学生端 logo（`student_logo_url`）。
- “学生端背景颜色”已应用到学生端主页面背景（不再仅限 guide 页）。

---

## 4. Student 端：舍友大厅与个人资料页

### 4.1 舍友大厅状态标签
- “其他同学”卡片左上角状态标签与真实队伍状态绑定：
  - `未组队`：绿色
  - `已组队`：黄色
  - `已满员`：红色
- 修复“其他同学”队伍人数缺失导致状态误判的问题（后端补充真实 `team_students_num`）。

### 4.2 个人资料页（`/roommates/:id`）
- 标题改为：`名字的个人资料`。
- “Ta 的队伍”中卡片状态角标改到左上角，并与真实队伍状态关联。
- 该页队伍满员判断改为读取系统配置 `team_max_student_count`（不再写死 4）。

---

## 5. Student 端：问卷填写页（`/questionnaire`）

- 布局调整：
  - 左侧“我的资料”与右侧“问卷调查”顶端对齐。
  - “分页”模块仍位于“我的资料”下方。
- “问卷调查”标题下新增当前分页备注显示（小号橙色字，来自 Admin 问卷分页备注）。
  - 备注仅在问卷填写页显示，不在舍友大厅展示。
- “我的权重”改名为：`权重`，并移动到“问题标题”同一行右侧。
- 在“权重”右侧新增：`权重占比`，计算公式为：
  - `当前权重 / 所有权重之和 * 100%`
- 权重输入规则调整：
  - 仅允许整数（步进改为 1）。
  - 上限为 20（范围 `0~20`）。
- 权重占比显示与控件宽度优化：
  - 数字显示改短（整数百分比，如 `35%`）。
  - “权重-权重占比”整体宽度缩窄。

---

## 6. test-data：本地联调数据工具

### 6.1 新增 test-data 目录
- 新增目录：`RMMT-API/test-data/`，用于一键生成本地联调数据。
- 主要文件：
  - `RMMT-API/test-data/seed_test_data.py`：测试数据导入脚本。
  - `RMMT-API/test-data/accounts.csv`：脚本生成的账号密码清单（管理员 + 学生）。
  - `RMMT-API/test-data/README.md`：test-data 使用说明。

### 6.2 导入脚本能力（`seed_test_data.py`）
- 问卷保障：
  - 若数据库没有问卷，自动生成 3 个分页、共 9 题（每页 3 题）。
- 业务数据重建（保留系统设置与问卷结构）：
  - 1 个管理员账号。
  - 20 个学生账号（10 男 + 10 女）。
  - 学生随机资料（`qq/wechat/contact/mbti/province`）。
  - 每个学生自动生成问卷答案与随机权重。
  - 每个性别队伍分布：4 人满队 + 2 人半队 + 4 人未组队。
- 资源与文件生成：
  - 自动生成学生头像到 `RMMT-API/static/uploads/student_avatar`。
  - 自动导出账号表到 `RMMT-API/test-data/accounts.csv`。

### 6.3 运行方式（本地）
- 在 `RMMT-API` 虚拟环境中执行：
  - `python test-data/seed_test_data.py --db-host 172.18.0.2 --db-port 3306 --db-user root --db-password <你的数据库密码> --db-name roommate`

